import { scope } from '../runner';

export default () => {
  scope('Discovery', () => {
    // TODO: Find Robo's location and implement the following

    // flow('User discovery via map', () => {

    // });

    // flow('User discovery via radar', () => {

    // });
  });
};
