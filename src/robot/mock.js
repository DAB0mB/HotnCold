import React from 'react';

const noop = () => {};

export const Robo = React.Fragment;
export const scope = noop;
export const flow = noop;
export const trap = noop;
export const before = noop;
export const beforeEach = noop;
export const after = noop;
export const afterEach = noop;
trap.use = noop;
